{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "debug",
            "type": "cppdbg",
            "request": "launch",
            "cwd": "${workspaceRoot}",
            "program": "${workspaceRoot}/prog",
            "filterStderr" : true,
            "preLaunchTask": "build"
            //"logging": { "engineLogging": true, "traceResponse": true }
        }
    ]
}